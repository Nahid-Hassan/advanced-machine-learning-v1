import cv2

image = cv2.imread('images/1.png')

# Convert image into gray scale, for doing this we use
# cv2.cvtColor(), which basically converts your image into different colour spaces..

gray_scale = cv2.cvtColor(image, )